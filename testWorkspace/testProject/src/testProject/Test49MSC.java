package testProject;

import java.security.SecureRandom;
import java.util.Random;

public class Test49MSC {
	@SuppressWarnings("unused")
	public void test() {
		
		// Test MSC02J
		// Uncomment next line to test
		Random rnd = new Random();
		rnd.nextInt();
		SecureRandom sRnd = new SecureRandom();
	}
}
